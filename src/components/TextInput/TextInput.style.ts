import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  input: {
    height: 35,
    width: 200,
    borderColor: '#d9d9d9',
    borderWidth: 1,
    borderRadius: 35,
    backgroundColor: '#f7f7f7',
  },
})
